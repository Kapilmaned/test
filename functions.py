def user_age_in_seconds():
    user_age=int(input("Enter your age"))
    age_seconds=user_age*365*24*60*60
    print(f"your age in seconds is {age_seconds}")

user_age_in_seconds()

# Avoid shadow  the name of variales
